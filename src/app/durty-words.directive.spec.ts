import { DurtyWordsDirective } from './durty-words.directive';

describe('DurtyWordsDirective', () => {
  it('should create an instance', () => {
    const directive = new DurtyWordsDirective();
    expect(directive).toBeTruthy();
  });
});
